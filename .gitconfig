[user]
	name = Sung-Kook Kim
	email = skkeem@gmail.com
[core]
	editor = vim
[credential]
	helper = cache
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
